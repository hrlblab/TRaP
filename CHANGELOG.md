# Changelog

## 2026-09-18 — Binning 输出轴不均匀（用户反馈）

- **Bug**：BinWidth 设为 1 cm⁻¹ 时，保存的波数轴间距时而 1、时而 2 cm⁻¹
- **根因**：`Binning` 把没接到任何数据点的 bin 直接丢弃。探测器在波长上均匀采样，换算到波数后间距随波数下降（本例 1.215 → 0.982 cm⁻¹，中位 1.087）。1.0 cm⁻¹ 的 bin 宽在整个 650–1700 窗口都低于本地采样间距，于是 1049 个 bin 里有 88 个落在两个采样点之间、什么都没接到，被丢掉后轴就变成 1/2 交替（654、660、666… 缺失）
- **修复**：内部空 bin 改为由相邻 bin 插值补上，保持输出轴严格均匀；只有落在数据覆盖范围之外的边缘空 bin 仍然丢弃。`Binning` 新增 `return_info=True`，返回补了多少、丢了多少
- **新增告警**：BinWidth 细于数据支持时，`run_pipeline` 抛 `BinWidthTooFine` 警告，说明补了几个 bin、以及实际的最大/中位采样间距。Batch UI 把这条写进处理日志（每次批处理只提示一次）
- **影响**：与参考 MATLAB 结果的相关性提升（最差 r 0.99881 → **0.99955**），因为后续 SG 滤波按索引工作、现在跨的是等间距。nRMSE 名义上从 1.28% 升到 1.45%，但其中几乎全部是 ~0.965 的整体增益 —— 扣掉增益后残差降到 0.44–0.73%。增益来自 mean 归一化对采样网格的依赖：补回的 bin 集中在低波数弱信号区，拉低了均值。峰位、峰强比不受影响
- **建议用法**：把 BinWidth 设到轴的**最大**采样间距以上（本例 ≥1.25 cm⁻¹），这样每个 bin 都只含实测点，不需要任何插值

## 2026-09-18
- **Cosmic ray removal**：`utils/CosmicRay.py` 新模块，Step 3 不再是空转。两个算法互补，按是否存在邻近测量选择
  - **Whitaker–Hayes (2018)**：单谱法，对一阶差分算 modified Z-score。差分消掉基线和荧光，尖峰成为 MAD 尺度下的极端离群点。判据是"窄"，需要真实谱带明显宽于尖峰
  - **Li–Dai (2011)**：双谱法，与同一样品的邻近测量做线性近似 `x = a·xm + b` 后比对。检测完全不依赖尖峰宽度，在尖峰与真实谱带同宽的低分辨率系统上仍然成立；强峰上的尖峰用移动窗口相关性分析与真实峰变化区分
  - Batch UI 新增"Replicate Group"选项：勾选后相邻文件互为参照谱。未勾选或只有单个文件时 Li–Dai 自动回退到 Whitaker–Hayes
  - 默认 `None`（保持原有行为）。所有参数暴露到 Batch UI 并写入配置
  - 替换的点数和位置写入处理日志，输出文件名带 `CR<method>` 标记
- **偏离论文的两处**（均已在代码注释说明原因）
  - Li–Dai 的 zs 分支：检测区窄于 `narrow_zone`（默认 3 px）时直接判为尖峰，跳过相关性检验。相关性对尺度不敏感，尖峰落在峰顶时 3 点窗口的形状不变、会被漏掉；而论文的 zs 区宽达数十像素，因为在线相邻谱的强峰本身在漂移
  - 相关性分析区改为对称扩展，原论文未规定窄区如何扩展
- **验证**：对 Megan 的 7 条重复谱注入合成尖峰（位置覆盖平坦区、弱信号区、强峰顶），Li–Dai 12 例中检出 11 例，Whitaker–Hayes 检出 6 例；两者在干净谱上均为零误报、尖峰外零失真。Whitaker–Hayes 的 `z_thresh` 默认 15.0，是在这 7 条谱上零误报的最低阈值

## 2026-09-17 — v1.0.6
- **修复光谱响应校正取错列**：`SpectralResponseCorrection` 之前读的是校正文件的第 0 列（波数），而不是第 1 列（校正因子），并且按行索引对齐而非按波数插值。对于 Renishaw 的两列 `WLCor_*.txt`（1015 点、降序、覆盖 650.5–1751 cm⁻¹）配上升序光谱（962 点、覆盖 650.9–1699 cm⁻¹），结果是把光谱乘上了一条波数斜坡 —— 低波数抬高、高波数压低。两个网格的点间距接近，但覆盖范围和点数不同，因此按行索引对齐无论排序方向都是错的
  - 新签名 `SpectralResponseCorrection(wlCorr, rawSpect, wvn=None)`：两列 `[波数, 因子]` 输入按波数 `np.interp` 插值到光谱轴上（对应 MATLAB 的 `interp1`）；单列因子仍按元素相乘
  - 移除 `[199:, 0]` 归一化常数：它除的是波数列，且校正数组短于 200 行时 `np.mean` 空切片返回 NaN，会污染整条谱。因子现按原样应用，与参考 MATLAB 实现一致
  - 长度不匹配改为抛 `ValueError`，不再静默错算
- **Batch 预览与实际运行对齐**：Renishaw 的 batch 预览此前跳过了 SRC 且未跳过暗基线，与 worker 行为不一致，预览结果和落盘结果对不上。现已一致
- **验证**：对 Megan 提供的 7 条血样光谱，与实验室参考 MATLAB 脚本（`Poly(5)`, `sg(2,11)`, `WLCor_626`）对比，Pearson r 从 0.809–0.859 提升至 0.9988–0.9994，最差 nRMSE 1.28%

## 2026-03-23
- **Renishaw 支持**：Renishaw 系统仍需 Spectral Response Correction（SRC），仅自动跳过 X 轴标定步骤
- **暗背景减除跳过**：Renishaw/显微镜系统跳过最小值暗基线减除（Step 1）
- **FBS 排除区域**：荧光背景拟合支持手动输入排除波数范围（冻结 Raman 峰区域，防止拟合偏移）
- **整数 Binwidth 对齐**：Binwidth 为整数时，网格自动偏移，使 bin 中点落在整数波数上
- **二次截断（Truncate2）**：在归一化前可选第二次截断，支持截取最终关注区域
- **步骤标题显示**：图表标题显示当前步骤编号与名称（`Current: Step N — StepName`）
- **比较线颜色修正**：Before 曲线从灰色改为钢蓝色（`#5B9BD5`），与 After 曲线区分更清晰
- **Renishaw 激光波长扩展**：Config Manager 支持 532 / 633 / 785 / 830 / 1064 nm
- **Batch Process 同步**：上述所有功能同步至 Batch Processing UI

## 2026-03-17
- **修复 DLL 加载错误**：其他机器运行 `TRaP.exe` 时报 `ImportError: DLL load failed while importing QtCore`
  - 添加 PyInstaller runtime hook（`pyi_rthook.py`），在启动时调用 `os.add_dll_directory(sys._MEIPASS)`
  - 显式打包 conda 环境中的 Qt5 DLL 文件（`Qt5*_conda.dll`）

## 2026-03-16
- 清理仓库：移除 IDE 文件、`__pycache__`、文档等无关文件
- 移除跟踪：`project_status.md`、`image.png`、`config.json`
- 更新 README 标题与单位信息
- **修复**：保存配置时非 Renishaw 系统的跳过复选框不再被错误重置
- **Renishaw 自动跳过**：检测到 Renishaw 系统时自动勾选跳过 X 轴标定
- **修复 Renishaw 数据**：波数从降序正确读取
- Config Manager 添加字段校验（加载/保存时）
- 添加 TRaP logo 至导航栏与窗口图标

## 2026-03-15
- 暴露 FBS 最大迭代次数和归一化方式参数
- 全面重构处理 UI 与配置校验逻辑
- 修复标定和 SRCF 对话框的垂直缩放问题
- 修复 SRCF 波长轴、图表 xlim；build 排除 torch 相关模块

## 2026-03-04
- **修复白光校正**：波长公式错误（`1e7/(1e7/λ - wvn)`）及元组解包问题
- **修复 Renishaw 波数轴**：从 0 开始的 X 轴问题（改用 `read_txt_file`）
- 添加交互式光标读数：图表上显示最近数据点的波数和强度

## 2026-02-25
- 修复 Batch 长输出路径处理问题

## 2026-02-23
- Batch 支持子目录输出，改进保存报错信息
- P-Mean 平滑步骤调整至背景减除之后

## 2026-02-20
- 修复 SRCF 输出格式
- 鲁棒文件读取器
- 自适应画布字体
- Build 修复

## 2026-02-19
- 自适应窗口尺寸
- 所有 UI 面板支持滚动

## 2026-02-17
- 重写 README（结构更清晰、内容更简洁）

## 2026-02-16
- 更新 Python 脚本，添加 `.gitignore`

## 2025-12-15
- 多项 UI 与功能更新

## 2025-10-30
- Wizard UI 更新

## 2025-09-29
- UI 更新

## 2025-08-18 — 2025-08-21
- Wizard UI 更新；综合更新

## 2025-07-07 — 2025-07-23
- 白光校正更新；Wizard UI 更新

## 2025-05-22 — 2025-06-24
- 早期开发，功能移除与更新
